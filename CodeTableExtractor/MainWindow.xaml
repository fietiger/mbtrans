<Window
    x:Class="CodeTableExtractor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="编码表提取工具"
    Width="600"
    Height="400"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!--  窗口标题输入  -->
        <Label
            Grid.Row="0"
            Grid.Column="0"
            VerticalAlignment="Center"
            Content="窗口标题:" />
        <TextBox
            Name="txtWindowTitle"
            Grid.Row="0"
            Grid.Column="1"
            Margin="5"
            Text="输入编码表详细视图 - 超强音形输入法" />
        <Button
            Grid.Row="0"
            Grid.Column="2"
            Width="60"
            Margin="5"
            Click="BtnFindWindow_Click"
            Content="查找" />

        <!--  结果显示  -->
        <Label
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Margin="0,10,0,5"
            Content="提取结果:" />
        <TextBox
            Name="txtResult"
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Margin="5"
            AcceptsReturn="True"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto" />

        <!--  进度显示区  -->
        <StackPanel
            Grid.Row="3"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>

                <ProgressBar
                    Name="progressBar"
                    Grid.Column="0"
                    Height="20"
                    Margin="0,0,10,5"
                    Maximum="100"
                    Minimum="0"
                    Value="0" />
                <TextBlock
                    Name="txtProgress"
                    Grid.Column="1"
                    Margin="0,0,10,5"
                    VerticalAlignment="Center"
                    Text="进度: 0/0"
                    TextAlignment="Center" />
                <TextBlock
                    Name="txtRunTime"
                    Grid.Column="2"
                    Margin="0,0,0,5"
                    VerticalAlignment="Center"
                    Text="运行时间: 00:00:00"
                    TextAlignment="Center" />
            </Grid>
        </StackPanel>

        <!--  操作按钮  -->
        <StackPanel
            Grid.Row="4"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Margin="10"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="5"
                Click="BtnFastExtract_Click"
                Content="快速提取" />
            <Button
                Width="100"
                Margin="5"
                Click="BtnClearLog_Click"
                Content="清空日志" />
        </StackPanel>
    </Grid>
</Window>
